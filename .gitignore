.zig-cache/
zig-out/
# SQLite amalgamation — generated during build setup, not committed
lib/
# Vendored C source (linenoise)
!lib/linenoise/
sqlite.zip
# Nix build output symlink
result
# Agent instructions and skills (local-only)
skills-lock.json
.agents/
.slim/
.slim/deepwork/
