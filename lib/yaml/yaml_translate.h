#ifndef YAML_TRANSLATE_H
#define YAML_TRANSLATE_H

#if defined(_MSC_VER)
#  define wcscat_s(dest, destsz, src) ((void)0)
#  define wcscpy_s(dest, destsz, src) ((void)0)
#endif

#include "yaml.h"

#endif
